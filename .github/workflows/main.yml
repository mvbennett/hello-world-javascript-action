on: [push]

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: A job to say hello
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Hello World Action Step
        uses: ./
        id: hello_world

      - name: Get Match Output
        run: echo "${{ steps.hello_world.outputs.match }}"
